{
  "name": "the-measure",
  "private": true,
  "description": "A menswear style diagnostic -- static site, no build step needed to run it. This file only exists to pin the dependency used by scripts/generate-images.js.",
  "scripts": {
    "generate-images": "node scripts/generate-images.js",
    "wire-images": "node scripts/wire-images.js",
    "build-pages": "node scripts/build-pages.js"
  },
  "dependencies": {
    "sharp": "^0.35.4"
  }
}
